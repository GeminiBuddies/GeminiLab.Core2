namespace GeminiLab.Core2.Consts {
    // temporarily internal
    internal static class SystemArray {
        public const int MaxArrayLength = 0X7FEFFFFF; // M$ makes `Array.MaxArrayLength` internal, so...
    }
}
